<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.calcmarket.viewmodels.BuysViewModel" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="@dimen/dimen_10"
        tools:context=".ui.fragments.BuyDetailFragment">

        <TextView
            android:id="@+id/title"
            style="@style/TextAppearance.AppCompat.Light.SearchResult.Title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAlignment="center"
            android:textSize="20sp"
            android:text="Detalle del mercado"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Detalle del mercado" />

        <TextView
            android:id="@+id/buyNameTextView"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/padding_20"
            android:text="@{viewModel.buySelectedLiveData.name}"
            android:textAlignment="viewStart"
            android:textSize="20sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/title"
            tools:text="Compra del dia 24 dfdsks;fdkse agosto 2023" />

        <TextView
            android:id="@+id/buyTotalTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:build_coin_format="@{viewModel.buySelectedLiveData.totalBuyValue}"
            android:textAlignment="center"
            android:textSize="15sp"
            app:layout_constraintStart_toStartOf="@+id/buyNameTextView"
            app:layout_constraintTop_toBottomOf="@+id/buyNameTextView"
            tools:text="Valor total $126.000" />

        <TextView
            android:id="@+id/buyCountItemsTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text_total_products="@{viewModel.buySelectedLiveData.itemsCount}"
            android:textAlignment="center"
            android:textSize="15sp"
            app:layout_constraintEnd_toEndOf="@+id/buyNameTextView"
            app:layout_constraintTop_toBottomOf="@+id/buyNameTextView"
            tools:text="Total de productos 3" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/productsRecyclerViewView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="@dimen/dimen_10"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/buyCountItemsTextView"
            tools:listitem="@layout/item_child_product" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>